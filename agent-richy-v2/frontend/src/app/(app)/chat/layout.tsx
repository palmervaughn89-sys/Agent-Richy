import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Chat with Richy | Agent Richy' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
