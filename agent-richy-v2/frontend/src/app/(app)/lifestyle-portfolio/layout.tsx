import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Lifestyle Portfolio | Agent Richy' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
